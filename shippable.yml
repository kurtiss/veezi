language: python
python:
  - 2.7.9
install:
  python setup.py install
before_script:
  mkdir -p shippable/testresults
script:
  - python setup.py test --with-xunit --xunit-file=shippable/testresults/nosetests.xml